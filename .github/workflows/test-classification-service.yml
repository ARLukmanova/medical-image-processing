name: test and list classification-service 

on:
  push:
    branches:
      - '**'
    paths:
      - 'classification-service/**'
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/test-and-lint-component.yml
    with:
      component-name: classification-service